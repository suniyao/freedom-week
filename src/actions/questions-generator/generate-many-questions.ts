"use server";

import {DifficultyRanking, Question} from "@/app/types";

export default async function generateManyQuestions(types: string[], difficulty: DifficultyRanking): Promise<Question[]> {
    return []
}